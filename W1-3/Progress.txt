[0407]
파싱하는 프로그램에 해당 출력 형식대로 해야되는건가?
blktrace를 .... 파싱 하는프로그램? blkparse처럼?????
우선 리눅스 자체 툴부터 살펴보기

[0410]
리눅스 툴 hdparm -> sudo hdparm --fibmap 파일명
filefrag -> filefrag -e 파일명
size 와 바이트와는 다른건가? 사이즈는 2660 인데 4096바이트
stat 이랑 filefrag 랑 hdparm 에서는 8개섹터 4096바이트로 나오는데 작성한 c파일이랑 섹터 수가 다르다 -> read부분 다시 작성하기
작성한 c 파일은 2660 사이즈에맞게 나눠서 나와서 6개섹터로 나옴,..... 뭐지?

[0412]
읽을 때는 26651160 ~ 26651165 까지만  6개 섹터
hdparm  26651160 ~ 26651167 까지해서 8개 섹터
blkparse 로 출력텍스트파일 만들고 그 파일에서 lba 읽어오기 -> 그 속에서도 pid 로 필터링하기
pid는 계속 0만 나오는데?

[0413]
complete 될때는 pid가 0 -> 스케줄러 관련되어서...?, 일단 이벤트 c 는 파싱하지 않고 d로만 분류해보기
내가 작성한건 지금 순서대로 읽고 있어서 lba 가 연속되어서 나오지만, 다른 io들은 굉장히 퍼져있다
내가 실행시킨 프로그램만 걸러내는 방법? 실행시킬때의 프로세스를 매개변수로 넘겨줘야되는건가??? 아닐것같은데
hdparm 과 size 가 다른 이유 -> 여러 사이즈로 바꿔보면서 규칙 발견해보기, 왠지 2의n승일 것 같다.

[0414]
일단 파싱하는 프로그램까진 만들어보았는데, 너무 종속적이다
파싱할 출력파일이름과 실제 읽는 파일에 대한 이름은 줄 수 있다 쳐도
blkparse 의 -f 로 주는 값이 고정적이어야한다는게 좀 그렇다 -> 한 라인씩 읽으면서 라인에서 어느 위치에 뭐가 있어야된다고 미리 설정
내가 읽는 파일에 대한 필터링을 지정해준 사이즈 1로 한다는것 -> 프로세스 아이디를 확인하는 방법을 모르겠음

[0426]
